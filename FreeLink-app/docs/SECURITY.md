# FreeLink Security Model

- End-to-end encrypted with $ \text{NaCl box (ChaCha20-Poly1305)} $
- Obfuscated handshake defeats DPI
- No traffic logs stored
- Public key pinning ensures server authenticity
- APK distributed via IPFS with SHA-256 verification

Node operators are expected to uphold zero-log policy.
